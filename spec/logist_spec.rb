require "spec_helper"

RSpec.describe Logist do
  it "has a version number" do
    expect(Logist::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
